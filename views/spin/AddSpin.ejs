<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Spin</title>
</head>

<body>
    <main id="main" class="main">
        <div class="pagetitle">
            <h1>Add Spin</h1>
            <nav>
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="index.html">Home</a></li>
                    <li class="breadcrumb-item">Spin</li>
                    <li class="breadcrumb-item active">Add Spin</li>
                </ol>
            </nav>
        </div>
        <!-- End Page Title -->

        <section class="section">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Add Spin</h5>

                            <form class="row g-3" action="/admin/user/spin/submit-spin" method="POST">
                                <!-- Select Job Title from result array -->
                                <div class="col-12">
                                    <label for="job_id" class="form-label">Select Job Title</label>
                                    <select class="form-control" id="job_id" name="job_id" required>
                                        <option value="" selected disabled>Select Job Title</option>
                                        <!-- Loop through result array to populate options -->
                                        <% result.forEach(job => { %>
                                            <option value="<%= job._id %>"><%= job.title %></option>
                                        <% }); %>
                                    </select>
                                </div>

                                <!-- Input for Spin Title -->
                                <div class="col-12">
                                    <label for="spin_title" class="form-label">Spin Title</label>
                                    <input type="text" class="form-control" id="spin_title" name="spin_title" required />
                                </div>

                                <!-- Submit Button -->
                                <div class="col-12">
                                    <button type="submit" class="btn btn-primary">Submit Spin</button>
                                </div>
                            </form>

                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>
    <!-- End #main -->
</body>

</html>
